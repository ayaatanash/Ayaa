using System;

namespace CorePortfolio01_AyaaTanash
{
    class Program
    {
        static void Main(string[] args)
        {

            //A = Payment amount/month
            //P = Principal
            //R = Interest rate/month
            //N  = Total number of payments

            double principal;
            double interest;
            int years;

            Console.WriteLine(1r + r);
            Console.WriteLine("Your payment amount per month is $");
            Console.WriteLine(principal);
            Console.WriteLine(interest);
            Console.WriteLine(years);

            Math.Pow(1r + r, n);
            Math.Pow(1r, n - 1);
            double paymentAmount = Math.Pow(1r + r, n) / Math.Pow(1r, n - 1);


        } // end of method
    } // end of class
} // end of namespace
